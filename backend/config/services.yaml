# yaml-language-server: $schema=../vendor/symfony/dependency-injection/Loader/schema/services.schema.json

# This file is the entry point to configure your own services.
# Files in the packages/ subdirectory configure your dependencies.
# See also https://symfony.com/doc/current/service_container/import.html

# Put parameters here that don't need to change on each machine where the app is deployed
# https://symfony.com/doc/current/best_practices.html#use-parameters-for-application-configuration
parameters:

services:
    # default configuration for services in *this* file
    _defaults:
        autowire: true # Automatically injects dependencies in your services.
        autoconfigure: true # Automatically registers your services as commands, event subscribers, etc.

    # makes classes in src/ available to be used as services
    # this creates a service per class whose id is the fully-qualified class name
    App\:
        resource: "../src/"

    # add more service definitions when explicit configuration is needed
    # please note that last definitions always *replace* previous ones

    # Repository bindings
    App\Domain\Repository\ApiSourceRepositoryInterface:
        class: App\Infrastructure\Persistence\DoctrineApiSourceRepository

    App\Domain\Repository\DocumentRepositoryInterface:
        class: App\Infrastructure\Persistence\DoctrineDocumentRepository

    App\Domain\Repository\DocumentVariableRepositoryInterface:
        class: App\Infrastructure\Persistence\DoctrineDocumentVariableRepository

    App\Domain\Repository\DocumentGenerationRepositoryInterface:
        class: App\Infrastructure\Persistence\Doctrine\Repository\DoctrineDocumentGenerationRepository

    App\Domain\Repository\TemplateRepositoryInterface:
        class: App\Infrastructure\Persistence\Doctrine\Repository\DoctrineTemplateRepository

    App\Domain\Repository\CodeProcessorRepositoryInterface:
        class: App\Infrastructure\Persistence\Doctrine\Repository\DoctrineCodeProcessorRepository

    # Configure arguments for services
    App\Application\Service\DocumentGenerationService:
        arguments:
            $generatedFilesPath: '%kernel.project_dir%/public/generated'

    App\Infrastructure\External\CodeExecution\JavaScriptSandboxService:
        arguments:
            $projectDir: '%kernel.project_dir%'

    App\Infrastructure\External\PdfGeneration\SnappyPdfService:
        arguments:
            $wkhtmltopdfBinary: '/usr/bin/wkhtmltopdf'
